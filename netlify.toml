[build]
  command = "bun run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "24"
  BUN_VERSION = "1.3.11"

# Astro config redirect causes visual reload in prod
[[redirects]]
  from = "/"
  to = "/en"
  status = 302
  force = true
