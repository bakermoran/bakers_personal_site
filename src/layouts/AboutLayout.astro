---
import Hero from '@/components/Hero.astro';
import Prose from '@/components/Prose.astro';
import Main from '@/layouts/Main.astro';

interface Props {
  title: string;
}

const { title } = Astro.props;
---

<Main title={title} size='none'>
    <Hero class:list="max-w-prose" />
    <Prose>
    <article class="mt-12">
        <slot />
    </article>
    </Prose>
</Main>
