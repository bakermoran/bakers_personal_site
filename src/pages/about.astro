---
import Layout from '@/layouts/Layout.astro';

const { Content, frontmatter } = await import('@/content/about.md');
---

<Layout title={frontmatter.title}>
    <article>
    <h1>{frontmatter.title}</h1>
    <Content />
    </article>
</Layout>