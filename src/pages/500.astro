---
import Main from '@/layouts/Main.astro';

interface Props {
  error: unknown;
}

const { error } = Astro.props;
---

<Main title="Home">
  <h1 class="text-zinc-400">Something went wrong</h1>
  <div>{error instanceof Error ? error.message : "Unknown error"}</div>
</Main>
