---
import Main from '@/layouts/Main.astro';
import Error from '@/components/Error.astro';

interface Props {
  error: unknown;
}

const { error } = Astro.props;
---

<Main title="Home">
  <Error error={error} title="500 - Internal Server Error" description="An unexpected error occurred. Please try again later." />
</Main>
