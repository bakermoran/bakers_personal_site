---
import Main from '@/layouts/Main.astro';

interface Props {
  error: unknown;
}

const { error } = Astro.props;
---

<Main title="Accueil">
  <h1 class="text-zinc-400">Quelque chose s'est mal passé.</h1>
  <div>{error instanceof Error ? error.message : "Erreur inconnue"}</div>
</Main>
